# Lumino

Build instructions
- Download [xmake](https://xmake.io/) on your machine
- Make sure that there is a raylib library file in `lib/raylib/src` (`libraylib.a` on Unix, `libraylib.dll` on Windows)
- Run `xmake run` in the base directory
